@stefgo:registry=https://npm.pkg.github.com
# No token is stored here: npm reads it from the NPM_TOKEN environment variable, which
# ci.yml, release.yml and the Dockerfiles set. Without this line npm sends no token and
# GitHub Packages answers 401.
//npm.pkg.github.com/:_authToken=${NPM_TOKEN}
